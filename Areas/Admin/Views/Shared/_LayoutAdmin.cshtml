@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@* File: _LayoutAdmin.cshtml *@
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>@ViewData["Title"] - Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        body {
            background-color: #f5f5f5;
            font-family: "Segoe UI", sans-serif;
        }

        .sidebar {
            height: 100vh;
            background-color: #7c0101;
            color: white;
            padding-top: 30px;
            border-top-right-radius: 40px;
            border-bottom-right-radius: 40px;
        }

            .sidebar a {
                color: white;
                text-decoration: none;
                display: block;
                padding: 15px 25px;
                font-size: 16px;
                border-radius: 0 20px 20px 0;
                margin-bottom: 5px;
            }

                .sidebar a:hover {
                    background-color: #8c0c0c;
                }

                .sidebar a.active {
                    background-color: #fff;
                    color: #7c0101;
                    font-weight: bold;
                    font-size: 18px;
                    box-shadow: inset 3px 0px 0px #b95757;
                }

            .sidebar .text-center img {
                width: 80px;
                height: 80px;
                border-radius: 50%;
                object-fit: cover;
                border: 3px solid #fff;
            }

        .dashboard-content {
            padding: 40px;
        }
    </style>
</head>
<body>
    <div class="container-fluid px-0">
        <div class="row">
            <!-- Sidebar -->
            <div class="col-md-2 sidebar">
                <div class="text-center mb-4">
                    <img src="https://randomuser.me/api/portraits/men/32.jpg" alt="Admin" />
                    <p class="mt-2">Admin</p>
                </div>
                <a asp-area="Admin" asp-controller="Dashboard" asp-action="Index">Dashboard</a>
                <a asp-area="Admin" asp-controller="Category" asp-action="Index">Categories</a>
                <a asp-area="Admin" asp-controller="Product" asp-action="Index">Products</a>
                <a href="@Url.Action("Index", "UserManagement", new { area = "Admin" })">User Management</a>
                <a href="@Url.Action("Index", "Orders", new { area = "Admin" })">Orders Management</a>
                <a href="@Url.Action("Index", "DesignRequest", new { area = "Admin" })">Design Request</a>
                <form asp-area="" asp-controller="Account" asp-action="Logout" method="post" class="px-3">
                    <button type="submit" class="btn btn-danger w-100 mt-2">
                        <i class="fas fa-sign-out-alt"></i> Logout
                    </button>
                </form>
            </div>

            <!-- Main Content -->
            <div class="col-md-10 dashboard-content">
                @RenderBody()
            </div>
        </div>
    </div>
    @RenderSection("scripts", required: false)
</body>
</html>
